// const cursorRound = document.querySelector(".cursor");
// const cursorPoint = document.querySelector(".cursor2");

// const cursorFunc = (e) =>{
//     const Y = e.clientY;
//     const X = e.clientX;

//     cursorRound.style.transform = `translate3d(${X}px, ${Y}px, 0)`;
  
//     cursorPoint.style.transform = `translate3d(${X}px, ${Y}px, 0)`;
// }

// window.addEventListener('mousemove', cursorFunc);